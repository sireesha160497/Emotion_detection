#Before running the code,
#Please Do run these commands
#open terminal and run
#pip install pandas
#pip install nltk
#pip install scikit-learn
#pip install requests
#pip install flask

#run WebInterface.py on Flask first
#then run ThinkerGui.py as a python code.

#make sure to not disturb emotions.csv
#wile using upload csv function make sure you have column names Text,True Emotion,Predicted Emotion
